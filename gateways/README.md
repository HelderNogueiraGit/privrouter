# Gateways Folder
This folder is used to store gateway configuration files. Gateways are used to connect externally using network interfaces on system. You can configure multiple if your system has multiple ones (wlan0, wlan1, eth0, eth1 ...)
